name: app_links_platform_interface
description: A common platform interface for the app_links plugin.
homepage: https://github.com/llfbandit/app_links/app_links_platform_interface
version: 0.1.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.0
